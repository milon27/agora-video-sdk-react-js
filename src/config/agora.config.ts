console.log({
  APP_ID: `${import.meta.env.VITE_APP_ID}`,
  channel: `${import.meta.env.VITE_CHANNEL}`,
});

export const AgoraConfig = {
  APP_ID: `${import.meta.env.VITE_APP_ID}`,
  channel: `${import.meta.env.VITE_CHANNEL}`,
};
